# Checklist funcional

Fuente estructurada: `docs/features.json`.

## Ruta crítica principal
- FTR-003
- FTR-004
- FTR-005
- FTR-006

## Estado actual
| ID | Función | Prioridad | Estado | Evidencias | Observaciones |
| --- | --- | --- | --- | --- | --- |
| FTR-003 | Se puede autenticar usuario y bloquear tras intentos fallidos | Alta | Verificada | `clinicdesk/app/security/auth.py`, `tests/test_auth_service.py`, `tests/test_login_dialog_ui.py`, `tests/test_session_controller.py`, `tests/test_main_auth_flow.py`, `tests/test_main_entrypoint_e2e.py` | La autenticación desktop ya cubre servicio, diálogo y transición post-login; además ahora existe un harness E2E/controlado del entrypoint real que ejecuta `main()` hasta `app.exec()`/salida limpia del loop Qt sobre SQLite temporal, con login aceptado, ventana principal visible y cierre controlado sin salida silenciosa. |
| FTR-004 | Se puede crear y consultar citas clínicas | Alta | Verificada | `clinicdesk/app/application/usecases/crear_cita.py`, `tests/test_citas.py`, `tests/ui/test_pacientes_viewmodel_puro.py`, `tests/ui/test_ruta_critica_desktop_smoke.py`, `tests/test_citas_security.py`, `tests/ui/test_citas_security_ui.py` | La ruta crítica desktop ya tiene smoke PySide6 con DB temporal que crea una cita por la vía UI soportada y la verifica en el listado. Además, la evidencia de seguridad ya cubre autorización real en el caso de uso, denial path sin persistencia parcial, guardrail UI readonly sin disparo del controller y auditoría saneada del flujo de creación/override. |
| FTR-005 | Se puede ejecutar pipeline ML de riesgo de citas | Alta | Verificada | `tests/test_ml_training.py`, `tests/test_train_calibration_integration.py`, `tests/test_drift_report.py`, `tests/ui/test_ruta_critica_desktop_smoke.py`, `tests/test_prediccion_operativa_facade_integracion.py`, `tests/test_main_entrypoint_e2e.py`, `tests/test_ml_cross_module_e2e.py`, `tests/test_prediccion_operativa_security_policy.py`, `tests/test_prediccion_operativa_security_static.py`, `tests/test_prediccion_operativa_security.py` | El flujo desktop de `prediccion_operativa` ya endurece seguridad real en la ruta residual: entrenamiento y reintento quedan protegidos por autorización explícita y centralizada con `Action.ML_ENTRENAR`, el usuario sin permiso recibe feedback observable sin disparar background, y la lectura/explicación permanece permitida para perfiles de solo lectura como decisión coherente con el modelo actual. La evidencia combina política de seguridad, guardrails estructurales y pruebas UI focalizadas del denial path. |
| FTR-006 | Se puede exportar KPIs y resultados en CSV | Alta | Verificada | `clinicdesk/app/application/usecases/export_kpis_csv.py`, `scripts/ml_cli.py`, `tests/test_export_kpis_csv.py`, `tests/test_export_kpis_csv_e2e.py` | La exportación CSV ya no queda solo en el caso de uso: una prueba E2E/controlada recorre el wiring real vía CLI con SQLite y stores temporales, ejecuta `seed-demo`, `build-features`, `train`, `score`, drift opcional y `export kpis`, y valida por contenido los cuatro artefactos contractuales más el error explícito ante requests inconsistentes. |
| FTR-007 | Se puede exportar auditoría de accesos con controles | Media | Verificada | `clinicdesk/app/application/usecases/exportar_auditoria_csv.py`, `tests/test_exportar_auditoria_csv_usecase.py`, `tests/test_auditoria_export_e2e.py` | La feature ya dispone de una prueba E2E/controlada sobre la UI real `PageAuditoria` dentro de `MainWindow`: carga la pantalla, aplica filtros válidos, dispara el job premium real de exportación, ejecuta el worker real con escritura en `tmp_path`, valida cabeceras contractuales/saneamiento de PII y confirma feedback observable tanto en éxito como cuando la integridad queda comprometida. |
| FTR-008 | Se puede rotar claves criptográficas de campos sensibles | Media | Verificada | `scripts/security_cli.py`, `tests/test_security_cli.py`, `tests/test_security_cli_e2e.py` | La CLI de seguridad ya queda verificada en modo E2E/controlado sobre SQLite temporal y wiring real: `generate-key`, `check-key`, `rotate-key --dry-run` y `rotate-key --apply`, más errores contractuales por modo ausente, entorno inválido, columnas de cifrado ausentes y schema/DB inválidos. La evidencia comprueba contadores, no fuga de PII/material criptográfico, recifrado real con lectura posterior por repositorio y auditoría saneada. |
